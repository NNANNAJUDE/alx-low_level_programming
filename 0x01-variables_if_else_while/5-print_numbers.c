#include <stdio.h>
/**
 * main - function to print numbernumbers to base starting from 0
 * Return: 0 if successful
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
